# Copy to .env for local frontend dev: cp .env.example .env
# Server-only secrets (GEMINI_API_KEY, STRIPE_*, service role) belong in
# Supabase Dashboard → Edge Functions → Secrets, not in this file.

VITE_PUBLIC_SITE_URL="https://www.coffee-bro.com"

VITE_SUPABASE_PROJECT_ID="your-project-ref"
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-key"
VITE_SUPABASE_URL="https://your-project-ref.supabase.co"

# Free at https://carto.com/basemaps/apikey — required to avoid CARTO tile watermarks
VITE_CARTO_BASEMAPS_API_KEY="your-carto-basemaps-key"